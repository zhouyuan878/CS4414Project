//contains all connection functions, manages p-p and p-s interactions, 
//when 'block counter' reaches a threshold, a block generated at a random spot on the map

use std::*

fn updateWorld(args: command) {
//matches the command and make the appropriate update to the world
}

fn updatePlayer(args: command, player) {
//matches the command and make the appropriate update to the player
}
